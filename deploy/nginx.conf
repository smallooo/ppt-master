# PPT Master Service — Nginx 反向代理示例
# 给小程序后端提供 HTTPS 入口（证书部分按需自行启用）。

upstream ppt_api {
    server api:8000;
}

server {
    listen 80;
    server_name _;

    # 微信小程序上传 PDF/PPT/图片可能很大；按需调高
    client_max_body_size 10m;

    # 上传/生成耗时较长，给 worker 足够时间
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;

    location /health {
        proxy_pass http://ppt_api/health;
    }

    location /api/ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://ppt_api;
    }
}

# 启用 HTTPS 时取消下方注释并替换证书路径
# server {
#     listen 443 ssl http2;
#     server_name your.domain.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     client_max_body_size 100m;
#     proxy_read_timeout 600s;
#     location /api/ { proxy_pass http://ppt_api; }
#     location /health { proxy_pass http://ppt_api/health; }
# }
